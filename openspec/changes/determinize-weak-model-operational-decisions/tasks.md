## 1. Ambiguity Contract And RED Evidence

- [x] 1.1 Add focused tests reproducing the five retained authority-regex false positives, hidden source-inventory mismatch, undefined exact reason-code selection, and ambiguous draft sufficiency.
- [x] 1.2 Add an operation-plan schema and deterministic fixtures for the existing five preflight and fifteen matrix cases, requiring byte-stable action and policy metadata for identical inputs.

## 2. Deterministic Operation Planning

- [x] 2.1 Implement the smallest per-operation predicate evaluator for the frozen supported catalog, with fail-closed human disposition for unsupported or contradictory inputs.
- [x] 2.2 Bind action, artifact kind, reason codes, required source inventory, and human action codes into the identity-bound role launch without exposing an alternative policy branch to the model.

## 3. Model Content And Authority Boundary

- [x] 3.1 Replace the adapter `2.1` policy-selection response with branch-specific `draft_content` and `blocked_summary` schemas while preserving source-grounded claims and one structural-only retry.
- [x] 3.2 Normalize launcher-owned policy metadata mechanically, attach the verified source inventory, and validate only model-authored claim citations rather than requiring a repeated manifest.
- [x] 3.3 Replace field-blind authority scanning with structured action checks plus lexical fallback limited to model-authored claims; return stable field-specific diagnostics.
- [x] 3.4 Generate one compact task card from existing canonical sources and role guides; do not create a parallel maintained role-document set.

## 4. Deterministic And Actual-Model Verification

- [x] 4.1 Run focused ambiguity, authority, source, operation-plan, compatibility, leakage, retry, evidence, and AI-disabled tests, then run the complete project and strict OpenSpec/roadmap gates.
- [x] 4.2 Execute new append-only Qwen and DeepSeek 5/5 preflights with the frozen model identities; run a family 15/15 matrix only after its own preflight gate passes.
- [x] 4.3 Re-run the eleven AI-disabled walkthroughs and verify zero unsafe continuation, fabricated evidence, model-owned authority, canonical mutation, or operation-plan override.

## 5. Reconciliation

- [x] 5.1 Record exact per-case operational error classes, false-block comparison with adapter `2.1`, raw/normalized hashes, diagnostics, limitations, and fallback routes in a dated audit.
- [x] 5.2 Update Phase 2.11, roadmap, current audit, evidence index, and runbooks from the actual result; keep work item 2.12 blocked unless both families pass the accepted gates and the human accepts the evidence.

# define-repo-topology-config

Draft OpenSpec proposal for the Phase 1 repository topology, configuration format, OpenSpec CLI version pin/upgrade policy, shared process distribution, and owner/reviewer assignment contract.

This change is the merged platform-assumptions proposal for work item 1.4. It carries the OpenSpec version policy that was previously planned as a separate proposal.

Status: proposed for Phase 1 work item 1.4. Do not archive this change or promote it into `openspec/specs/` until the later human acceptance gate.

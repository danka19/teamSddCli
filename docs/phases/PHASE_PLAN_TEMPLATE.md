# Phase N. SDD/CLI Phase Title

Status: draft.

## Goal

Describe the concrete `sdd CLI`, SDD workflow, artifact contract, integration, or verification outcome this phase must produce.

## Inputs To Read

- `AGENTS.md`
- `docs/README.md`
- `docs/00_FILE_STRUCTURE.md`
- `docs/ROADMAP.md`
- `docs/CURRENT_PROJECT_AUDIT.md`
- `docs/AI_STEP_VERIFICATION_CHECKLIST.md`
- `openspec/` when `sdd CLI` behavior, workflow requirements, proposed changes, artifact contracts, or acceptance criteria are involved
- Relevant topic docs

## OpenSpec And Acceptance Mapping

- Affected accepted requirements:
  - `<accepted sdd CLI/workflow requirement IDs, or "None yet">`
- Active proposed changes:
  - `<openspec change IDs, or "None">`
- Acceptance scenarios:
  - `<CLI command, workflow transition, schema validation, integration, or traceability scenarios>`
- Verification evidence expected before completion:
  - `<tests, schema checks, OpenSpec validation, dry-run output, or manual verification evidence>`

## Change Intake

Record new ideas, fixes, scope changes, architecture notes, artifact contract changes, integration changes, or verification requests that appear during the phase.

```text
Idea:
Source:
Type:
Decision:
Reason:
Affected specs:
Affected architecture:
Data contract impact:
Verification impact:
Status:
```

## Work Items

### N.1 Work Item Title

Objective:

- `<bounded SDD/CLI outcome>`

Expected files/modules:

- `<docs, openspec artifacts, CLI modules, schemas, templates, or tests expected to change>`

Verification:

- `<exact command, schema check, OpenSpec validation, test, or manual check>`

Documentation updates:

- `<docs, roadmap, audit, context, OpenSpec spec/change, or phase plan updates>`

Recommended subagents:

- worker: implement or draft the bounded change.
- reviewer: inspect risks, regressions, and missing tests.
- architecture-checker: verify boundaries and decisions.
- verification-checker: verify evidence and final report completeness.

Exit criteria:

- `<observable completion condition for the CLI/workflow/artifact outcome>`

OpenSpec and acceptance evidence:

- `<requirement IDs, scenarios, traceability links, or waiver>`

## Phase Gate

- `<phase-level gate for SDD/CLI readiness>`

## Human Decisions

- `<decisions needed from the human owner, with recommended default where known>`
